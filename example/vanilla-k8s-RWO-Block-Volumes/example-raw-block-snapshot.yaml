apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: example-raw-block-snapshot
spec:
  volumeSnapshotClassName: example-raw-block-snapshotclass
  source:
    persistentVolumeClaimName: example-raw-block-pvc
