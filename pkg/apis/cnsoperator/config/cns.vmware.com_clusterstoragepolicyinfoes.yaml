---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: clusterstoragepolicyinfoes.cns.vmware.com
spec:
  group: cns.vmware.com
  names:
    kind: ClusterStoragePolicyInfo
    listKind: ClusterStoragePolicyInfoList
    plural: clusterstoragepolicyinfoes
    shortNames:
    - clusterspi
    singular: clusterstoragepolicyinfo
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterStoragePolicyInfo is the Schema for the clusterstoragepolicyinfoes
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ClusterStoragePolicyInfoSpec defines the desired state of ClusterStoragePolicyInfo.
              It is used to store the information about the storage policy that is exposed to Devops users.
            properties:
              k8sCompliantName:
                description: K8sCompliantName is the Kubernetes-compliant name the
                  VI admin chose when creating the policy.
                type: string
            required:
            - k8sCompliantName
            type: object
          status:
            description: ClusterStoragePolicyInfoStatus defines the observed state
              of ClusterStoragePolicyInfo.
            properties:
              encryption:
                description: Encryption describes encryption-related status for the
                  storage policy.
                properties:
                  encryptionType:
                    description: EncryptionType indicates the type of encryption.
                    items:
                      enum:
                      - vm-encryption
                      - vsan-encryption
                      type: string
                    type: array
                  error:
                    description: Error describes the error encountered while retrieving
                      the encryption info.
                    type: string
                  supportsEncryption:
                    description: SupportsEncryption indicates whether the storage
                      policy supports encryption.
                    type: boolean
                type: object
              error:
                description: Error describes the error encountered while retrieving
                  the storage policy info.
                type: string
              performance:
                description: Performance describes performance characteristics (vSAN
                  only).
                properties:
                  error:
                    description: Error describes the error encountered while retrieving
                      the performance info.
                    type: string
                  iopsLimit:
                    description: IopsLimit is the IOPS limit for volumes created from
                      this storage class (vSAN ESA only).
                    format: int64
                    type: integer
                type: object
              storagePolicyDeleted:
                description: StoragePolicyDeleted indicates whether the underlying
                  storagepolicy is deleted or not on the VC.
                type: boolean
              volumeCapabilities:
                description: VolumeCapabilities describes the supported volume capabilities.
                items:
                  description: VolumeCapability describes capabilities of the volume
                    created with the given policy.
                  enum:
                  - PersistentVolumeBlock
                  - PersistentVolumeFilesystem
                  - HighPerformanceLinkedClone
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
