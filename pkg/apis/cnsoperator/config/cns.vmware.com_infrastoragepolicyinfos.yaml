---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: infrastoragepolicyinfos.cns.vmware.com
spec:
  group: cns.vmware.com
  names:
    kind: InfraStoragePolicyInfo
    listKind: InfraStoragePolicyInfoList
    plural: infrastoragepolicyinfos
    shortNames:
    - infraspi
    singular: infrastoragepolicyinfo
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          InfraStoragePolicyInfo is the Schema for the infrastoragepolicyinfos API.
          Name of this CR is same as the unique and immutable K8sCompliantName of the storage policy.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InfraStoragePolicyInfoSpec defines the desired state of InfraStoragePolicyInfo.
            properties:
              clusterStoragePolicyInfoRef:
                description: ClusterStoragePolicyInfoRef points to the corresponding
                  cluster-scoped ClusterStoragePolicyInfo.
                properties:
                  apiGroup:
                    description: APIGroup is the API group and version of the referenced
                      object, i.e. "cns.vmware.com/v1alpha1".
                    type: string
                  kind:
                    description: Kind is the kind of the referenced object, i.e. "ClusterStoragePolicyInfo".
                    type: string
                  name:
                    description: |-
                      Name is the name of the referenced ClusterStoragePolicyInfo. It is always equal to
                      this object's own name, since both share the same K8sCompliantName of the storage policy.
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
            required:
            - clusterStoragePolicyInfoRef
            type: object
          status:
            description: InfraStoragePolicyInfoStatus defines the observed state of
              InfraStoragePolicyInfo.
            properties:
              error:
                description: Error describes a failure condition when observing or
                  reconciling this resource.
                type: string
              topology:
                description: Topology contains observed topology for this storage
                  policy in the cluster.
                properties:
                  accessibleZones:
                    description: AccessibleZones lists zones where the policy is accessible
                      for this cluster.
                    items:
                      minLength: 1
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  topologyType:
                    description: |-
                      TopologyType describes the type of topology for the storage policy.
                      Valid values are empty (no topology) or "zonal" (zone-based topology).
                    enum:
                    - ""
                    - zonal
                    type: string
                required:
                - accessibleZones
                - topologyType
                type: object
              volumeCapabilities:
                additionalProperties:
                  type: boolean
                description: VolumeCapabilities describes the supported volume capabilities.
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
