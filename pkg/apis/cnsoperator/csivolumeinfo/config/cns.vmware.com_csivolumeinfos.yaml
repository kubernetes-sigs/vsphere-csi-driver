---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  creationTimestamp: null
  name: csivolumeinfos.cns.vmware.com
spec:
  group: cns.vmware.com
  names:
    kind: CsiVolumeInfo
    listKind: CsiVolumeInfoList
    plural: csivolumeinfos
    shortNames:
    - cvi
    singular: csivolumeinfo
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ownership
      name: Ownership
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.vms
      name: VMs
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CsiVolumeInfo tracks the ownership lifecycle of a CSI volume
          for the VM-owned volume attach/detach model. One CR exists per PV in the
          vmware-system-csi namespace.
        properties:
          apiVersion:
            description: APIVersion defines the versioned schema of this representation
              of an object.
            type: string
          kind:
            description: Kind is a string value representing the REST resource this
              object represents.
            type: string
          metadata:
            type: object
          spec:
            description: CsiVolumeInfoSpec defines the desired state of CsiVolumeInfo.
            properties:
              diskPath:
                description: DiskPath is the datastore path to the VMDK file.
                type: string
              diskUUID:
                description: DiskUUID is the stable identifier of the virtual disk.
                type: string
              pvName:
                description: PVName is the name of the bound PersistentVolume.
                type: string
              pvcName:
                description: PVCName is the name of the bound PVC.
                type: string
              pvcNamespace:
                description: PVCNamespace is the namespace of the bound PVC.
                type: string
              vmName:
                description: Deprecated - use vms instead.
                type: string
              volumeID:
                description: VolumeID is the CNS volume ID / PV volumeHandle. Immutable
                  after creation.
                minLength: 1
                type: string
              vms:
                description: VMs lists the VirtualMachine CRs that have this volume
                  attached.
                items:
                  description: VirtualMachineRef identifies a VM attached to the volume.
                  properties:
                    vmInstanceUUID:
                      description: VMInstanceUUID is the instance UUID of the VM.
                      type: string
                    vmName:
                      description: VMName is the VirtualMachine CR name.
                      type: string
                  required:
                  - vmName
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - vmName
                x-kubernetes-list-type: map
            required:
            - pvName
            - pvcName
            - pvcNamespace
            - volumeID
            type: object
          status:
            description: CsiVolumeInfoStatus defines the observed state of CsiVolumeInfo.
            properties:
              conditions:
                description: Conditions is a standard K8s condition array.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating
                        details about the transition.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation
                        that the condition was set based upon.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating
                        the reason for the condition's last transition.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              error:
                description: Error holds the last reconcile error message, if any.
                type: string
              observedGeneration:
                description: ObservedGeneration is the spec.generation last acted
                  on by the controller.
                format: int64
                type: integer
              ownership:
                description: Ownership is the current ownership state of the volume.
                enum:
                - CSIManaged
                - VMManaged
                type: string
              phase:
                description: Phase reflects the last reconcile outcome.
                enum:
                - Pending
                - Succeeded
                - Failed
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
